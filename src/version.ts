/**
 * minimax-proxy 版本号（semver）。用于启动日志与 /healthz 的 version 字段，
 * 供自动更新检测比对。发布时手动递增。
 *   - 自动随机签到、守护式拉起桌面端等为本版新增功能。
 *
 * @module minimax-proxy/version
 */
export const MINIMAX_CONNECT_VERSION = '1.4.2'
